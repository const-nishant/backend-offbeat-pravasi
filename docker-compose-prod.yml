services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    container_name: offbeat-api-prod
    restart: always
    ports:
      - '4000:4000'
    env_file: .env

networks:
  default:
    name: coolify
    external: true
