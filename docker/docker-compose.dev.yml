version: '3.9'
services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: dev
    env_file:
      - ../.env
    ports:
      - "${PORT:-4000}:4000"
    depends_on:
      - postgres
      - redis
    volumes:
      - ../:/workspace:cached
    command: npm run start:dev
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-offbeat}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  pgdata:

